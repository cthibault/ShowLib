<UserControl x:Class="ShowLib.Client.WPF.View.ShowsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:mui="http://firstfloorsoftware.com/ModernUI"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">

    <Grid Style="{StaticResource ContentRoot}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,5">
            <Button Content="Add" Command="{Binding AddNewShowCommand}" />
            <Button Content="Delete" Margin="3,0" Command="{Binding DeleteShowCommand}" />
        </StackPanel>
        
        <mui:ModernTab Grid.Row="1" Layout="List" Links="{Binding Links}" />
    </Grid>
</UserControl>
